package br.com.corretor.enums;

/**
 * Enum que define os recursos bancários suportados pela integração
 */
public enum RecursoBancario {
    SALDO,
    EXTRATO,
    WEBHOOK,
    PAGAMENTO,
    TRANSFERENCIA,
    PIX,
    BOLETO
}
